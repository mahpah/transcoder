{
  "name": "@mahpah/configuration",
  "version": "1.0.0",
  "description": "Simple configuration loader",
  "main": "dist/index.js",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prebuild": "rm -Rf dist",
    "build": "tsc -p tsconfig.json",
    "prepare": "npm run build"
  },
  "keywords": [],
  "author": "mahpah",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^8.5.2",
    "typescript": "^2.6.2"
  }
}
